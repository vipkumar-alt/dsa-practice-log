class Solution {
public:
    int findMin(vector<int>& nums) {
        int low=0;
        int high=nums.size()-1;
        int ans=0;

        while(low<high){
            int mid=low+(high-low)/2;

            if(nums[mid]>nums[high]){
                low=mid+1;
                ans=nums[mid];
            }
            else {
                high=mid;
                ans=nums[high];
            }
        }

        return ans;
    }
};
